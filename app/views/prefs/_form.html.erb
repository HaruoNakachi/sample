<%= form_for(@pref) do |f| %>
  <% if @pref.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pref.errors.count, "error") %> prohibited this pref from being saved:</h2>

      <ul>
      <% @pref.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
